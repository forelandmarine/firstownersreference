# Marketing material

Loose promotional and prospecting files for the publication. The strategy documents themselves live in `docs/` (`marketing-plan.md`, `social-strategy.md`, `social-calendar.md`, `seo-strategy.md`, `reddit-seeding.md`, `press-package.md`).

`Foreland_TFOR_family_office_prospects.xlsx` is the family office prospect list. This is a working copy; the original stays in iCloud at Foreland Group, so update both or treat iCloud as the master.

`tfor-live-nokicker.png` is a capture of the live site without the kicker, kept for social use.

The social card generator is a separate repository at `~/firstownersreference-social` and is deployed independently on Vercel. It is not consolidated here because it is its own deployment.
